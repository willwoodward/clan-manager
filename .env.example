# ============================================================================
# CLASH OF CLANS API CONFIGURATION
# ============================================================================

# CoC API Key (for REST API access)
# Get your API key from https://developer.clashofclans.com/
# IMPORTANT: Add your IP address to the allowed IPs list when creating the key
COC_API_KEY=your_api_key_here
VITE_CLASH_API_KEY=your_api_key_here  # For frontend (deprecated, use backend proxy)

# CoC Developer Account (for data-collector using coc.py)
COC_EMAIL=your_email@example.com
COC_PASSWORD=your_password_here

# Clan Configuration
CLAN_TAG=#29U8UJCU0
VITE_CLAN_TAG=#29U8UJCU0  # For frontend (deprecated, use backend)

# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================

# Storage Backend
# Set USE_S3=true when S3 is set up, otherwise uses local file storage
USE_S3=false
S3_BUCKET=clan-manager-wars
S3_PREFIX=wars
S3_REGION=us-east-1
LOCAL_DATA_DIR=data

# ============================================================================
# DATA COLLECTION OPTIONS (data-collector service)
# ============================================================================

# What to include in war data exports
INCLUDE_TROOPS=false
INCLUDE_SPELLS=false
INCLUDE_HEROES=true
INCLUDE_ONLY_MAX_LEVEL=false

# War monitoring schedule (cron format)
# Default: "0 0 * * *" = daily at midnight
CHECK_SCHEDULE_CRON=0 0 * * *

# Debug mode (fetches war data immediately instead of scheduling)
DEBUG_MODE=false

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================

# Server settings
API_HOST=0.0.0.0
API_PORT=8000

# CORS origins (comma-separated or JSON array)
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Cache settings (optional - improves performance)
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================

# Backend API URL
VITE_API_URL=http://localhost:8000

# ============================================================================
# DEVELOPMENT NOTES
# ============================================================================

# To run the full stack locally:
# 1. Data Collector (optional, for collecting new war data):
#    cd data-collector && python war_monitor.py --debug
#
# 2. Backend API:
#    cd backend && uvicorn main:app --reload
#
# 3. Frontend:
#    cd frontend && npm run dev
